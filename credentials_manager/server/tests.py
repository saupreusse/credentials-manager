import rotator

label = "webappcr"
newpassword = "nRhYPMhk+k-^qIOz"

rotator.finishRotation(label,newpassword)
#rotator.testConnection(label)
#rotator.setPassword(label, newpassword)
#rotator.testConnection(label, newpassword)